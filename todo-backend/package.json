{
  "name": "todo-backend",
  "version": "1.0.0",
  "description": "Lambda handler for the todo app API. TypeScript, deployed via CDK `NodejsFunction`.",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.140",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
